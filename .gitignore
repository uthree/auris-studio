# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/


# Added by cargo

/target

# Rendered exports and scratch audio
/exports
*.wav.tmp

# Projects composed while trying something out. The specifications in `examples/` are the
# source; a `.auris` beside them is the output, and a two-hundred-kilobyte note list is not
# something to review a diff of.
*.auris

# The SoundFonts the application ships with, fetched by `tools/fetch-soundfonts.sh`. Two hundred
# megabytes of samples that GitHub would refuse and every clone would pay for; the manifest in
# `auris_session::library` is what is version-controlled instead.
/SoundFonts

# The Japanese dictionary, fetched by `tools/fetch-dictionary.sh` for the same reason; its
# manifest lives beside the fonts' in `auris_session::library`.
/Dictionary

# macOS
.DS_Store

# The vendored fork has no dependencies of its own; its lock file is regenerated by any
# `cargo` command run from its directory and says nothing worth keeping.
vendor/rustysynth/Cargo.lock

# Per-machine MCP registration: it names an absolute path to a locally built binary,
# so each machine writes its own. See docs/features.md for the one-line setup.
.mcp.json
